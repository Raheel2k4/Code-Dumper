# Code-Dumper
Python script to recursively walk a folder, skip common unwanted directories, and concatenate all text/code files into a single code_dump.txt (including folder tree + file contents). Ideal for sharing codebases with LLMs or for manual review.
